[phases.setup]
nixPkgs = ["python310", "python310Packages.pip"]

[phases.install]
cmds = ["pip install ."]

[start]
cmd = "uvicorn agent_joko.dashboard.api:create_app --factory --host 0.0.0.0 --port $PORT"
